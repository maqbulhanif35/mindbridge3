import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder test', (WidgetTester tester) async {
    // Placeholder — add real integration tests once Supabase is connected.
    expect(1 + 1, equals(2));
  });
}
